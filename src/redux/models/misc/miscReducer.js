// Initial State
import { INITIAL_STATE } from '../../../common/app-const';

const admin = (state = INITIAL_STATE.misc, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default admin;
